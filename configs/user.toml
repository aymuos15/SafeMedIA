# User DP Style (Server-side clipping)
[data]
data_dir = "/home/localssk23/CAI4Soumya/SegData/nnUNet_raw/Dataset001_Cellpose"
image_size = 256
batch_size = 8

[model]
in_channels = 1
out_channels = 2
channels = [16, 32, 64, 128]
strides = [2, 2, 2]
num_res_units = 2

[federated]
num_clients = 2
num_rounds = 2
min_fit_clients = 2
min_evaluate_clients = 2
min_available_clients = 2

[training]
local_epochs = 1
learning_rate = 0.001

[privacy]
style = "user"
[privacy.user]
noise_multiplier = 0.5
max_grad_norm = 0.1
